---
description: "Прогон модели B на задаче бенчмарка. Читает task.md, изучает код проекта, пишет ответ в data/answers/T-NNN/modelB.md"
---

Загрузи скилл `benchmark-run-b` через tool `skill` с `id: benchmark-run-b` и выполни его полностью, сам сделай анализ и запиши ответ.

Перед началом прочитай `AGENTS.md`.

Аргументы: $ARGUMENTS

Если аргумент — task-id вида T-NNN, используй его. Без него используй `current_task` из `data/settings.yaml`, иначе последнюю созданную задачу.
