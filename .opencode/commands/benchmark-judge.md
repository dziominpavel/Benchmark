---
description: "Судья бенчмарка. Проверяет modelA.md и modelB.md по исходникам и рубрике задачи, объявляет победителя"
---

Загрузи скилл `benchmark-judge` через tool `skill` с `id: benchmark-judge` и выполни его полностью в отдельном независимом сравнении.

Перед началом прочитай `AGENTS.md`.

Аргументы: $ARGUMENTS

Если аргумент — task-id вида T-NNN, используй его. Без него используй `current_task` из `data/settings.yaml`, иначе последнюю задачу с двумя ответами.
