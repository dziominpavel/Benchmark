---
# Front matter задачи. Заполни при создании.
# id — в формате <PREFIX>-NNN (F-001, B-002, R-003, V-004). Уникален в рамках категории.
# category — feature | bugfix | refactor | review
# difficulty — 1 (простая) | 2 (средняя) | 3 (сложная)
# project — gymprogress | voicemind | wishlot | multi | none
# baseline_commit — SHA коммита проекта, на котором задача актуальна.
#                   Участник делает git checkout <baseline_commit>.
#                   Никаких git-тегов в проектах не требуется.
# estimated_minutes — грубая оценка для человека (необязательно)
# tags — свободные теги для фильтрации (например: compose, room, parser, ui, alarm)

id: F-NNN-<slug>
title: "<Краткое название задачи>"
category: feature
difficulty: 2
project: voicemind  # ИЛИ projects: [voicemind, gymprogress] для multi-project
baseline_commit: <40-символьный SHA из git rev-parse HEAD>
estimated_minutes: 60
tags: [compose, ui]
---

# <Краткое название задачи>

## Контекст

Опиши, в каком состоянии находится проект/модуль, к которому относится задача.
Сошлись на спеки/docs проекта, которые модель должна прочитать перед началом:
- `C:/projects/<Project>/AGENTS.md`
- `C:/projects/<Project>/docs/<file>.md`
- `C:/projects/<Project>/openspec/specs/<capability>/spec.md`

## Постановка

Чётко и однозначно: что модель должна сделать. Без двусмысленностей.
Если это фича — какие требования/сценарии. Если баг — какие шаги воспроизведения
и ожидаемое поведение. Если рефакторинг — какой модуль и какая цель.
Если review — что именно разобрать и что вернуть.

## Критерии приёмки

- [ ] Критерий 1 (проверимый)
- [ ] Критерий 2
- [ ] Критерий 3

## Ограничения

Что модель НЕ должна делать (например: «не менять схему Room без миграции»,
«не добавлять новые зависимости»,«не трогать экраны кроме X»).

## Подсказки

Необязательный раздел: наводки, ссылки на похожие места в коде,
порядок действий. Если подсказок нет — удали раздел.

## Оценка

Оценивается по единому чек-листу `tasks/_CHECKLIST.md`.
Каждый пункт 0–5 баллов. Итог = сумма.
Для категории `review` часть пунктов чек-листа может быть неприменима —
помечай `N/A` в прогоне.
