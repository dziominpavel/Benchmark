## 1. Слоты и судья

- [x] 1.1 Убрать поле `model` из шаблонов ответов (`data/answers/_TEMPLATE.md`, примеры в `benchmark-run-a/b`) и проверить `openspec validate --all` проходит
- [x] 1.2 Обновить skills `benchmark-run-a`, `benchmark-run-b` (писать только `{task, date}`) и `benchmark-judge` + `docs/judge-prompt.md` (слот = имя файла, шапку игнорировать), проверить чтением обновлённых файлов
- [x] 1.3 Прогнать ручную проверку судьи на паре со старой шапкой внутри и убедиться оценка не блокируется

## 2. Чистка авто-учёта

- [x] 2.1 Убрать скан `answers/` из `collect_tasks_info`, поле `answers` из `index.json` и `has_answer`/плашку «нужны ответы» из сервера, проверить `python tools/elo.py` пересчитывает индекс без поля `answers`
- [x] 2.2 Убедиться рекомендации пар работают без флагов ответов и `openspec validate --all` проходит

## 3. Ручной учёт (бэк)

- [x] 3.1 Добавить `data/answer_flags.yaml` и функции загрузки/сохранения с игнором неизвестных id, проверить round-trip скриптом
- [x] 3.2 Добавить построение матрицы и сводки долгов (дефолт false, архивные/неактивные скрыты) и проверить на реестре с новой моделью

## 4. Ручной учёт (UI)

- [x] 4.1 Добавить сводку и матрицу с чекбоксами, сортировкой должников сверху и кнопкой сохранения в настройках, проверить сохранение Quadro через браузер
- [x] 4.2 Добавить фильтр «только долги» и bulk по строке/столбцу, проверить скрытие закрытых строк

## 5. Сквозная проверка

- [x] 5.1 Прогнать `uv run python tools/test_elo.py`, `python tools/elo.py --check` и `openspec validate --all`, убедиться всё зелёное
