## 1. Helpers без Flask

- [x] 1.1 Добавить в `tools/render_helpers.py` `get_model_detail(index_data, model_id)`: `None` для неизвестного id; иначе имя, текущий ELO/статистика из кэша, точки графика (1200 + `after` своих матчей по `seq`), H2H по соперникам (встреч/W/L/D + `format_winrate`), личная история (соперник/дата/исход/до/Δ/после, исход из `winner` и сторон); проверить: прямым вызовом на живом `data/index.json` для модели с историей и для модели с 0 игр
- [x] 1.2 Добавить в `tools/render_helpers.py` `build_elo_svg(points)`: ломаная старт + точки, подписи min/max/текущий, вырожденный диапазон раскрывается, пустой вход → заглушка «Нет матчей»; проверить: SVG-строка содержит полилинию и три подписи, заглушка для `[]`

## 2. Сервер: рекомендации и таблица

- [x] 2.1 Добавить в `tools/server.py` `format_reason(rec)` (`не встречались · Δ=N` / `счёт A:B · Δ=N` / суффикс ` · новая модель` при `tier==0`) и подстановку `reason` в рекомендации внутри `leaderboard()`; вывод `get_recommendations` не менять; проверить: `test_pairing.py` зелёный, в карточке видна строка-обоснование
- [x] 2.2 В `INDEX_TEMPLATE`: строка-обоснование под `rec-h2h`; легенда «Старт 1200 · K 40/32/24 · ничья 0.5» у строки статистики; медали 🥇🥈🥉 + класс строки для `top3_ids` (роут вычисляет, фильтр `inactive` — без медалей); имя модели → `/model/<id>` + ссылка «изменить»; секция `<details>` «Архивные модели (N)» в режиме `active`; проверить: главная рендерится, все элементы на месте

## 3. Страница модели и финал

- [x] 3.1 Добавить роут `/model/<model_id>` + `MODEL_TEMPLATE` (шапка, график, H2H, история, «← Назад»; 404 для неизвестного id); проверить: страница модели с историей и модели с 0 игр открываются, 404 работает
- [x] 3.2 Финальная проверка: `test_elo.py`, `test_pairing.py`, `elo.py --check` зелёные; `openspec validate "model-page-and-pairing"` валиден; главная, `/model/<id>`, `/history`, `/settings` отвечают 200
