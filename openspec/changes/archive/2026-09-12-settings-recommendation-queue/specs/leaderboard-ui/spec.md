## ADDED Requirements

### Requirement: Очередь рекомендаций в настройках

Страница `/settings` SHALL содержать карточку «Очередь рекомендаций» во всю ширину страницы, расположенную после секции «Наличие ответов». Карточка SHALL показывать единый упорядоченный список всех открытых ячеек `(таск, пара)` в порядке рекомендаций (приоритет активного таска `T-NNN` по возрастанию, затем `pair_games` по возрастанию, затем тир калибровки по возрастанию, затем близость ELO по убыванию). Каждая строка SHALL содержать порядковый номер начиная с 1, пару моделей в виде «Имя A vs Имя B» и таск ячейки. Имена моделей SHALL быть ссылками на страницы моделей. Шапка карточки SHALL показывать счётчик «осталось N», где N — число строк списка, и сводку «закрыто D из T» из покрытия. Длина списка SHALL равняться остатку покрытия (`total − filled`). BLOCKED-ячейки (без обоих ответов) SHALL NOT показываться в списке. Карточка SHALL пересчитываться при каждом открытии страницы без кэша.

#### Scenario: Открытие очереди при частичном покрытии

- **WHEN** покрытие составляет 145 закрытых из 181 и пользователь открывает `/settings`
- **THEN** карточка показывает заголовок со счётчиком 36
- **AND** список содержит 36 строк в порядке рекомендаций

#### Scenario: Порядок совпадает с кнопкой Прогнать

- **WHEN** первая строка очереди — пара A-B с таском `T-001`
- **THEN** топ-1 рекомендации на главной предлагает ту же пару A-B с тем же таском `T-001`

#### Scenario: Строка очереди

- **WHEN** очередь содержит ячейку `(T-001, {A, B})` под номером 1
- **THEN** строка показывает «1», «Имя A vs Имя B» и «T-001»
- **AND** имена ведут на страницы моделей

#### Scenario: Скрытые долги не входят в список

- **WHEN** у пары A-B нет ответа одной из моделей на `T-002` и вердикта нет
- **THEN** ячейка `(T-002, {A, B})` SHALL NOT появляться в списке

#### Scenario: Пустая очередь при полном покрытии

- **WHEN** открытых ячеек нет (`осталось 0`), но есть топ-1 рематч
- **THEN** карточка показывает пустое состояние «Очередь пуста»
- **AND** ниже показывает одну строку рематча с пометкой «рематч»

#### Scenario: Пустая очередь без видимых ячеек

- **WHEN** видимых ячеек нет вообще (все BLOCKED, активных моделей не менее 2)
- **THEN** карточка показывает пустое состояние
- **AND** счётчик показывает прочерк вместо числа
