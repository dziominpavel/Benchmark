## 1. Бэкенд очереди

- [x] 1.1 Прокинуть полную очередь в `settings_page` через `get_recommendations` с верхней границей и проверить, что первая строка совпадает с топ-1 главной и `len(queue) == total − filled`
- [x] 1.2 Прокинуть флаг рематча и пустой `recommended_task` для пустого состояния и проверить рендер без падения при отсутствии активных тасков

## 2. Шаблон настроек

- [x] 2.1 Добавить полноширинную карточку «Очередь рекомендаций» после «Наличия ответов» (счётчик, сводка, таблица `№ | Пара | Таск`, ссылки на модели) и проверить открытием `/settings` при частичном покрытии
- [x] 2.2 Добавить пустое состояние и строку рематча с пометкой «рематч» и проверить при полном покрытии и при всех BLOCKED

## 3. Стили и проверка

- [x] 3.1 Добавить скролл-обёртку списка на существующих классах таблицы и проверить на широком и узком viewport без ломки layout
- [x] 3.2 Запустить `openspec validate --all` и ручную сверку `/settings` с покрытием и главной, проверить отсутствие регрессий существующих секций
