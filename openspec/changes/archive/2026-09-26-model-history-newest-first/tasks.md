## 1. Порядок личной истории в `get_model_detail()`

- [x] 1.1 В `tools/render_helpers.py` инвертировать список, идущий под ключом
      `history` (разворот после цикла сбора, `points` и `mine` оставить в
      исходном порядке `seq` ASC) и добавить комментарий о разнонаправленности
      `points`/`history`. Проверка: прямой вызов `get_model_detail()` на живом
      `data/index.json` для модели с ≥2 матчами — первый элемент `history`
      совпадает с последним по `seq` элементом `mine`, а `points` длиной
      `1 + games` и остаётся возрастающим по времени
- [x] 1.2 Обновить докстринг `get_model_detail()`: `history` — в обратном
      хронологическом порядке (новые сверху), `points` — в хронологическом.
      Проверка: текст докстринга не противоречит фактическому порядку
      возвращаемых списков

## 2. Спека

- [x] 2.1 Проверить, что delta `openspec/changes/model-history-newest-first/specs/leaderboard-ui/spec.md`
      содержит полный блок `### Requirement: H2H-таблица и личная история`
      (оба исходных сценария сохранены + два новых) и открывается
      `openspec show "leaderboard-ui" --type spec`. Проверка:
      `openspec validate "model-history-newest-first"` проходит без ошибок
- [x] 2.2 После применения изменений убедиться, что в main-спеке
      `openspec/specs/leaderboard-ui/spec.md` порядок строк истории стал
      обратным, а Requirement «SVG-график динамики ELO» («в порядке `seq`»)
      остался без изменений. Проверка: `openspec validate --strict` без
      предупреждений по этому требованию

## 3. Проверка на живом UI

- [x] 3.1 Запустить сервер (`python tools/server.py`) и открыть страницу модели
      с историей: первая строка «История матчей» — самый свежий матч по `seq`,
      последняя — самый старый. Проверка: визуально даты убывают сверху вниз
- [x] 3.2 Убедиться, что SVG-график «Динамика ELO» остался хронологическим:
      ломаная идёт слева направо от старта 1200 к текущему ELO, подписи min/max
      и «текущий» остались на своих местах. Проверка: график не отражён
      по горизонтали относительно состояния до правки
- [x] 3.3 Сверить с `/history`: оба окна показывают новые записи сверху.
      Проверка: первая строка `/model/<id>` и первая строка `/history`
      относятся к одному и тому же последнему вердикту
- [x] 3.4 Проверить модель без матчей (пустая история) и H2H-секцию:
      пустое состояние «Пока нет матчей» и порядок строк H2H не изменились.
      Проверка: H2H по-прежнему отсортирован по убыванию встреч, затем по имени

## 4. Регрессия

- [x] 4.1 Прогнать `uv run python tools/test_elo.py` — движок ELO не затронут,
      все проверки зелёные. Проверка: вывод завершается без ошибок
- [x] 4.2 Сверить состояние репозитория: изменены только
      `tools/render_helpers.py` и артефакты change; `data/index.json`,
      `data/models.yaml`, журнал `data/matchups/` не тронуты.
      Проверка: `git status --short` показывает ожидаемый набор файлов
