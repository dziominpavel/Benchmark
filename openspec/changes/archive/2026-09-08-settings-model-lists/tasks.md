## 1. Данные в роуте

- [x] 1.1 В `settings_page` (`tools/server.py`) переключиться с `load_models_yaml()` на `ensure_index()`, собрать `models_active_list` и `models_archived_list` (словари `id`, `name`, `elo`), каждый отсортирован по `elo` desc; счётчики считать по этим спискам и передать списки в `render_template_string`. Проверка: страница `/settings` открывается без ошибок, счётчики совпадают с прежними значениями.

## 2. Шаблон SETTINGS_TEMPLATE

- [x] 2.1 В карточке «Модели» заменить таблицу из трёх строк на блок-список: строка «Всего в реестре» (обычная) и два `<details>` — «Активных» и «Неактивных (архив)», свёрнутые по умолчанию. Проверка: в отрендеренном HTML есть `<details>` без атрибута `open`, `<summary>` содержит подпись и счётчик.
- [x] 2.2 Внутри каждого `<details>` вывести список моделей группы: `<a href="/model/<id>">` с именем и ELO. Проверка: при раскрытии видны все модели группы по убыванию ELO, ссылки ведут на `/model/<id>`.
- [x] 2.3 Для пустой группы внутри `<details>` вывести текст-заглушку (например, «Нет неактивных моделей»). Проверка: при `models_archived_list` пустом раскрытие показывает заглушку.
- [x] 2.4 Стилизовать `<details>`/`<summary>` под вид прежних строк таблицы (переиспользовать классы `tasks-table`/существующий CSS, при необходимости добавить минимальный CSS в блок `CSS`). Проверка: визуально карточка выглядит как раньше в свёрнутом виде.

## 3. Проверка

- [x] 3.1 Открыть `/settings` в браузере и пройти сценарии из спеки: счётчики свёрнуты, «Активных» раскрывается в список по ELO desc, клик по имени ведёт на `/model/<id>`, пустая группа показывает заглушку. Проверка: все сценарии выполняются.
- [x] 3.2 Прогнать `openspec validate settings-model-lists` (и при наличии тестов для server.py — соответствующий pytest). Проверка: валидация и тесты зелёные.
