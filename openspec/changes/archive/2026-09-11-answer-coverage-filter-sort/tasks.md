## 1. Матрица ответов

- [x] 1.1 Изменить сортировку строк в `get_answer_matrix` на алфавит по имени и проверить сценарий «Строки по алфавиту» (модель с долгами ниже модели без долгов, если имя позже)
- [x] 1.2 Обновить docstring `get_answer_matrix` (порядок строк) и проверить чтением файла, что упоминание «должники сверху» убрано

## 2. Фильтр в секции «Наличие ответов»

- [x] 2.1 Добавить в `settings_page` чтение и валидацию `answer_filter` (`active`/`all`/`inactive`, по умолчанию `active`) с фильтрацией строк и проверить сценарии фильтра по умолчанию, «Все» и «Только неактивные»
- [x] 2.2 Добавить в `SETTINGS_TEMPLATE` селект «Показывать» (GET, `onchange=submit`), единый список с пилюлей «неактивна» вместо details-таблицы архивных и пустые состояния, и проверить рендер всех трёх режимов и сохранение `answer_filter` в URL
- [x] 2.3 Сохранять `answer_filter` при редиректах после POST (`/answer_flags`, toggle/current тасков) и проверить, что после сохранения фильтр не сбрасывается

## 3. Сохранение без потерь

- [x] 3.1 Доработать `save_answer_flags_route` для сохранения флагов моделей вне фильтра из existing и проверить сценарий «Сохранение не теряет скрытые флаги» (флаг архивной пережил сохранение в режиме `active`)
- [x] 3.2 Проверить JS bulk-действия (`только долги`, счётчик, строка/столбец) поверх отфильтрованного списка и пустые состояния

## 4. Приёмка

- [x] 4.1 Прогнать `openspec validate --all` и проверить отсутствие ошибок по изменённым спекам
- [x] 4.2 Прогнать `uv run python tools/test_elo.py` и проверить зелёный результат
