## 1. Серверный UI

- [x] 1.1 В `tools/server.py` добавить CSS-классы для 2-column layout (`.main-layout` 60/40, media-query <900px), сворачиваемой истории и пагинации.
- [x] 1.2 Переверстать `INDEX_TEMPLATE`: header, stats и filter-bar вверху, затем левая колонка (leaderboard), правая колонка (рекомендации + форма вердикта), затём сворачиваемый блок истории с пагинацией.
- [x] 1.3 В route `/` добавить параметр `page` и формирование `history` с пагинацией по 20 записей, а также переменных для навигации (`history_page`, `history_pages`).
- [x] 1.4 Обновить JS: удалить ненужный `scrollIntoView` для «Прогнать», добавить обработку пагинации истории (page numbers, GET-ссылки).
- [x] 1.5 Запустить сервер и проверить: две колонки, адаптив, история сворачивается и пагинируется по 20 записей.

## 2. Статический экспорт

- [x] 2.1 В `tools/generate_html.py` добавить CSS для 2-column layout, сворачиваемой истории и JS-пагинации.
- [x] 2.2 Обновить HTML-шаблон: leaderboard слева, справа панель без формы с пометкой о сервере, снизу сворачиваемая пагинированная история.
- [x] 2.3 Сгенерировать `leaderboard.html` и проверить layout, сворачивание и переключение страниц истории.

## 3. Спецификация

- [x] 3.1 Обновить `openspec/specs/leaderboard-ui/spec.md` (через sync/archive): двухколоночный layout, сворачиваемая пагинированная история, панель добавления результата.
