## 1. Счётчик игр пары и сортировка

- [x] 1.1 В `tools/server.py` заменить `get_all_matchup_pairs()` на функцию, возвращающую `dict[frozenset, int]` — число применённых вердиктов пары (tombstone и аннулированные не считаются). Проверка: юнит-проверка или ручной прогон — пара с 1 вердиктом даёт count=1, аннулированный вердикт не увеличивает счётчик.
- [x] 1.2 В `get_recommendations()` убрать исключение сыгранных пар, добавить `pair_games` в кандидаты и в выходной dict, сменить сортировку на `(pair_games, tier, -score)`, добавить ветку причины `pair_games > 0` → «рематч (встречались N раз)». Проверка: на текущих данных топ-3 содержит только несыгранные пары (поведение без изменений до конца круга).
- [x] 1.3 В `INDEX_TEMPLATE` заменить/удалить плашку «Все пары уже прогнаны…» — пустой список теперь означает <2 активных моделей. Проверка: визуально — блок не показывает плашку при наличии пар.

## 2. Тесты и валидация

- [x] 2.1 Расширить `tools/test_pairing.py`: сценарий «все пары сыграны → рекомендуются рематчи с min pair_games» и существующий сценарий «архивная модель исключена» проходит с новым счётчиком. Проверка: `python tools/test_pairing.py` — exit 0.
- [x] 2.2 Прогнать `python tools/test_elo.py` и `python tools/test_pairing.py` — регрессий нет; `openspec validate --all` — дельта-спеки валидны.
