## 1. Хелпер доверия

- [x] 1.1 Добавить `confidence_half_width(games)` в `tools/render_helpers.py` (`0 → None`, иначе `round(400 / sqrt(games))`) и проверить на `games = 0/4/13/19` значения `—/200/111/92`
- [x] 1.2 Покрыть хелпер сценариями спеки (прогрев при `<10`) ручным прогоном `python -c` без изменения данных

## 2. Таблица лидерборда

- [x] 2.1 Вывести колонку `±` и бейдж `прогрев` в `INDEX_TEMPLATE` (`tools/server.py:leaderboard`), сортировка по ELO без изменений, и проверить на главной: модель с 4 играми `±200 + прогрев`, с 19 — `±92` без бейджа
- [x] 2.2 Обновить легенду под таблицей на `Старт 1200 · K 40/32/24 · ничья 0.5 · ± = 400/√игр · прогрев <10 игр` и проверить строку на главной
- [x] 2.3 Проверить фильтры `active/all/inactive`, медали топ-3 и строку `Архивные модели (N)` — поведение как до изменения

## 3. Регрессия

- [x] 3.1 Прогнать `python tools/test_elo.py` и `uv run python tools/elo.py --check`, убедиться что журнал и `index.json` не менялись (зелёные прогоны, без новых полей)
