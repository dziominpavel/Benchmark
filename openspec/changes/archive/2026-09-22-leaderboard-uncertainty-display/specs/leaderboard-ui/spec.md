## ADDED Requirements

### Requirement: Индикатор доверия рейтинга в таблице лидерборда

Таблица лидерборда SHALL показывать рядом с ELO колонку доверия `±` и бейдж `прогрев`. `±` SHALL вычисляться только из числа игр модели как `round(400 / sqrt(games))` при `games >= 1`, при `games == 0` SHALL показываться `—`. Модель с `games < 10` SHALL помечаться бейджем `прогрев`. Сортировка таблицы SHALL оставаться по убыванию ELO; колонки `±` и `прогрев` SHALL NOT влиять на ранжирование, медали топ-3 и фильтры. Формула ELO, журнал вердиктов и `index.json` NOT меняются — `±` считается на лету при рендере.

#### Scenario: Модель в прогреве

- **WHEN** модель имеет `elo = 1281` и `games = 4`
- **THEN** в таблице показано `1281 ±200` и бейдж `прогрев`

#### Scenario: Устоявшаяся модель

- **WHEN** модель имеет `elo = 1181` и `games = 19`
- **THEN** в таблице показано `1181 ±92` без бейджа `прогрев`

#### Scenario: Модель без игр

- **WHEN** модель имеет `games = 0`
- **THEN** в колонке доверия показано `—`

#### Scenario: Порядок не меняется

- **WHEN** модель A имеет `1397 ±111` (13 игр), модель B — `1368 ±111` (13 игр)
- **THEN** A стоит выше B по ELO
- **AND** интервалы перекрываются, разница читается как шум, а не как доказанный отрыв

### Requirement: Легенда доверия

Легенда под таблицей лидерборда SHALL иметь вид `Старт 1200 · K 40/32/24 · ничья 0.5 · ± = 400/√игр · прогрев <10 игр`.

#### Scenario: Легенда видна

- **WHEN** пользователь открывает главную
- **THEN** под таблицей видна строка `Старт 1200 · K 40/32/24 · ничья 0.5 · ± = 400/√игр · прогрев <10 игр`
