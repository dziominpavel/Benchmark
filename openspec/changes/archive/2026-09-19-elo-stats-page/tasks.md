## 1. SVG распределения

- [x] 1.1 Переписать хелпер распределения Elo в `tools/render_helpers.py` на горизонтальные бары по моделям (имя + значение, раскраска ниже/выше 1200, линии 1200/среднее, пустое состояние) и проверить рендером на данных `data/index.json` (20 баров, 10 ниже 1200)
- [x] 1.2 Проверить ось и читаемость на реальных данных: ось от минимума с паддингом, имена и значения видны, линии 1200/среднего различимы

## 2. Страница статистики

- [x] 2.1 Добавить роут `/stats` в `tools/server.py` (сводка N/ниже-1200/среднее из `index.json`, фильтр active/all по query-параметру с дефолтом active) и проверить открытием страницы без параметров — выбраны только активные
- [x] 2.2 Добавить ссылку «Аналитика» в шапку главной и ссылку «Назад к рейтингу» на `/stats` и проверить переходы в обе стороны
- [x] 2.3 Добавить карточку «Распределение Elo» с фильтром «Показывать» (только активные / все) и проверить пересчет графика и сводки при переключении, включая сохранение фильтра в URL

## 3. Проверка

- [x] 3.1 Прогнать `openspec validate --all` и проверить отсутствие ошибок по ченджу
- [x] 3.2 Открыть `/stats` при пустом фильтре (нет моделей) и проверить пустое состояние вместо графика
