## 1. Правки шаблонов в tools/server.py

- [x] 1.1 В `INDEX_TEMPLATE` удалить ссылку «изменить» из ячейки «Модель» таблицы рейтинга (рядом с `{{ m.name }}`, ~строка 957); проверить: главная страница рендерится, в ячейках нет ссылки на `/edit/`
- [x] 1.2 В `MODEL_TEMPLATE` добавить в `detail-header` кнопку «Изменить» (`<a href="/edit/{{ detail.id }}" class="btn btn-secondary">`) рядом с именем/ELO; проверить: `/model/<id>` показывает кнопку, ведущую на форму редактирования
- [x] 1.3 В `MODEL_TEMPLATE` и роуте `model_page` добавить вывод алертов `error`/`success` из query-параметров (как в других шаблонах); проверить: `/model/<id>?success=...` показывает сообщение
- [x] 1.4 В `EDIT_TEMPLATE` перенаправить ссылку «← Назад к рейтингу» и кнопку «Отмена» на `/model/{{ model.id }}` (текст «Назад» скорректировать под новый адресат); проверить: обе ссылки ведут на страницу модели

## 2. Редирект после сохранения

- [x] 2.1 В роуте `edit_model` (POST `/edit/<model_id>`) заменить редирект на `leaderboard` редиректом на `model_page` с параметром `success`; проверить: после сохранения открывается `/model/<id>` с сообщением об успехе

## 3. Проверка

- [x] 3.1 Прогнать существующие тесты (`python -m pytest tools/test_elo.py tools/test_pairing.py`) и убедиться, что регрессий нет
- [x] 3.2 Запустить сервер и вручную проверить сценарий: главная без ссылок «изменить» → страница модели → «Изменить» → сохранение → возврат на страницу модели с сообщением
