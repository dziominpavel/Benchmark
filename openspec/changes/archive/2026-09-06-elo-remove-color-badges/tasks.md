## 1. Серверный UI

- [x] 1.1 Убрать CSS-правила `.elo-badge`, `.elo-high`, `.elo-mid`, `.elo-low` из `<style>` в `tools/server.py`.
- [x] 1.2 Заменить ELO-бейдж в шаблоне главной таблицы на обычное число: убрать `<span class="elo-badge ...">` вокруг `{{ m.elo }}`.
- [x] 1.3 Заменить ELO-бейдж в шаблоне страницы `/add` на обычное число.
- [x] 1.4 Запустить сервер и проверить, что ELO в таблице — обычное число без цвета.

## 2. Статический HTML-экспорт

- [x] 2.1 Убрать CSS-правила `.elo-high`, `.elo-mid`, `.elo-low` из `tools/generate_html.py`.
- [x] 2.2 Убрать расчёт `elo_class` и вывод `{elo}` без класса в `ROW_TEMPLATE`.
- [x] 2.3 Запустить `python tools/generate_html.py` и проверить, что `leaderboard.html` не содержит цветных ELO-значений.

## 3. Спецификация

- [x] 3.1 Обновить `openspec/specs/leaderboard-ui/spec.md` (через архив дельты или sync) — убрать упоминание цветных бейджей из сценария `Отображение рейтинга`.
