# Tasks: settings-and-task-progress

## 1. Настройки: хранение и доменная логика (tools/elo.py)

- [x] 1.1 Добавить `load_settings()` / `save_settings()` в `tools/elo.py`: чтение `settings.yaml` (отсутствует → дефолты `current_task: general`, `tasks: {}`), запись YAML без BOM; проверка — юнит-тест: запись и чтение возвращают те же значения.
- [x] 1.2 Добавить `known_tasks()`: объединение директорий `matchups/`, id из `tasks/T-NNN-*` (правило `collect_tasks_info`) и ключей `settings.yaml.tasks`; проверка — функция возвращает `general`, `T-001` на текущем репозитории.
- [x] 1.3 Добавить `active_tasks(settings)` и `resolve_current_task(settings)`: таска без записи → active; `current_task` неактивен/неизвестен → `general`; проверка — юнит-тесты граничных случаев.
- [x] 1.4 Добавить `get_coverage()` → `(заполнено, всего, процент|None)`: один проход по `load_matchups()` с пропуском tombstone/аннулированных (логика `get_pair_stats`), фильтр по активным таскам и активным моделям, кап 100%; проверка — юнит-тест: 3 игры пары в одном таске дают 1 ячейку; вердикт в неактивной таске и с архивной моделью не учитывается; 0 активных тасков или <2 моделей → `None`.

## 2. Веб-UI: страница настроек (tools/server.py)

- [x] 2.1 Создать `SETTINGS_TEMPLATE`: плашка «Добавить модель» → `/add`, плашка «Таски» (таблица известных тасков: чекбокс «активна» + радио «текущая» только для активных), кнопка «Сохранить», ссылка «← Назад»; проверка — GET `/settings` отдаёт страницу со списком `general`, `T-001`.
- [x] 2.2 Роут `POST /settings`: парсинг чекбоксов/радио → `save_settings()`; `current_task` не из активных → ошибка или сброс на `general`; редирект с `success`; проверка — после POST в `settings.yaml` корректные статусы и `current_task`.
- [x] 2.3 Шапка главной: кнопка «+ Добавить модель» → «Настройки» (`/settings`); обновить текст пустого состояния таблицы («Нет моделей») на ссылку через настройки; проверка — в HTML главной есть `href="/settings"` и нет кнопки `/add` в шапке.

## 3. Веб-UI: форма вердикта и прогресс (tools/server.py)

- [x] 3.1 В форму «Записать результат» добавить поле «Таск»: `<input name="task" list="task-list" value="{{ current_task }}">` + `<datalist>` из `known_tasks()`; расширить `validate()` — кнопки блокируются при пустом таске; проверка — в HTML поле предзаполнено `current_task`, datalist содержит известные таски.
- [x] 3.2 `POST /verdict`: принимать `task`, отклонять пустое/невалидное значение с ошибкой, передавать в `record_verdict(task=...)` вместо `DEFAULT_TASK`; проверка — запись с `task=T-001` создаёт `matchups/T-001/NNN.json`, невалидный таск → ошибка без файла.
- [x] 3.3 В строку статистики добавить «Прогресс: NN% (закрыто X из Y)» из `get_coverage()`; при `None` → `—`; проверка — главная показывает процент, соответствующий формуле на текущих данных (61 из 91 при одной активной таске ≈ 67%).

## 4. Проверка

- [x] 4.1 Создать `settings.yaml` в корне с текущими значениями (`general`, `T-001` — active; `current_task: general`) и закоммитить вместе с кодом; проверка — файл валидный YAML, `git status` показывает его неигнорируемым.
- [x] 4.2 Сквозная проверка: запустить `python tools/server.py`, открыть главную и `/settings`, записать вердикт с таском `general` и с `T-001`, отметить таску неактивной — прогресс и счётчики меняются корректно; `index.json` пересчитывается.
- [x] 4.3 Прогнать `openspec validate settings-and-task-progress --strict` — без ошибок.
