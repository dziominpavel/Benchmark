## 1. Рекомендации (pairing-algorithm)

- [x] 1.1 Перевести `get_recommendations` в `tools/server.py` на READY-ячейки `(task, pair)` с приоритетом таска (`T-NNN` → `pair_games` → тир → closeness) и топ-1; проверить вызовом в отдельной копии: при BLOCKED-паре в `T-001` и READY в `T-002` возвращается ячейка `T-001`
- [x] 1.2 Добавить режим рематчей (READY пуст): кандидаты без учёта флагов, сортировка `pair_games` → тир → closeness, `recommended_task` — первый активный таск, признак режима для пометки; проверить в отдельной копии: при 100% возвращается 1 рематч с признаком режима
- [x] 1.3 Сохранить поля записи (`h2h_label`, `score`, запрет поля `reason`) и `format_reason`; проверить регрессом существующих сценариев подписей `h2h_label` в отдельной копии

## 2. Покрытие (run-progress)

- [x] 2.1 Перевести `get_coverage` в `tools/elo.py` на видимые ячейки (`TOTAL = READY + DONE`, BLOCKED исключены, отсутствие `answer_flags.yaml` = все BLOCKED, снятие флага прячет ячейку целиком) с cap 100%; проверить в отдельной копии: D без ответов не меняет 6/6, флаг `D x T-001` даёт 6/9
- [x] 2.2 Зафиксировать инвариант `READY > 0 → percent < 100%` и невлияние повторов/рематчей на счёт; проверить вычислением на фикстуре с дублями вердиктов в отдельной копии

## 3. Главная страница (leaderboard-ui)

- [x] 3.1 Заменить блок рекомендаций на одну карточку топ-1 (имя, ELO, `h2h_label`, обоснование, «таск: T-NNN», «Прогнать» подставляет тройку), удалить `→`/счётчик/`showRec`/`nextRec`, добавить пометку «доп. точность: рематч» только в режиме рематчей без напоминаний про долги; проверить рендером главной в отдельной копии (топ-1, рематч, `< 2` моделей)
- [x] 3.2 Перевести статус-бар на динамическое покрытие («закрыто X из Y», «осталось N» = READY, `—` при `percent = None` включая «все BLOCKED», «Все пары закрыты» при 100%, cap 100%); проверить рендером трёх состояний в отдельной копии

## 4. Тесты

- [x] 4.1 Дополнить `tools/test_pairing.py` сценариями: BLOCKED-невидимость, приоритет `T-001` над `T-002`, топ-1, рематч-fallback без флагов; проверить запуском `uv run python tools/test_pairing.py` в отдельной копии
- [x] 4.2 Дополнить `tools/test_elo.py` сценариями: динамика знаменателя (новая модель, появление/снятие флага), cap 100%, повторы не влияют, все BLOCKED → `percent = None`; проверить запуском `uv run python tools/test_elo.py` в отдельной копии

## 5. Проверка change

- [x] 5.1 Прогнать `openspec validate --all` и устранить замечания по новым дельта-спекам; проверить чистым выводом команды
- [x] 5.2 Прогнать оба тестовых файла целиком и вручную открыть главную в отдельной копии (топ-1 кликабелен, бар и 100%/`—` корректны); проверить совпадением с инвариантом «есть рекомендация READY → < 100%»
