# Пул судей
#
# Зарезервировано под список model_id (из models.yaml), которые выступают
# в роли судей. Сейчас пул не используется:
#   - автоматическое судейство выполняет skill benchmark-judge
#     (.opencode/skills/benchmark-judge/SKILL.md + зеркала в .cursor/, .devin/);
#   - ручное судейство — по шаблону docs/judge-prompt.md в чате с любой LLM,
#     результат записывается через веб-UI (localhost:5000, выбор активной таски).
#
# Историческая справка: файл описывал несуществующий скрипт assign_judges.py
# (назначение 3 судей на прогон) — скрипта в репозитории нет, описание удалено.
# Если автоматическое назначение судей понадобится, spec — openspec/specs/.

judges:
