# Leaderboard

> Автоматически генерируется скриптом `tools/aggregate.py`.
> Не редактируй вручную — изменения будут перезаписаны.
> Запуск: `python tools/aggregate.py`

_Нет прогонов. После создания файлов в `runs/` запусти скрипт снова._
